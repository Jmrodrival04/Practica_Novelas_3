<resources>

    <!-- Tema claro -->
    <style name="Theme.PracticaNovelas3" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores principales para el tema claro -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Configuración del fondo y los textos -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:navigationBarColor">@color/purple_500</item>
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <!-- Tema oscuro -->
    <style name="Theme.PracticaNovelas3.Dark" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colores principales para el tema oscuro -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Configuración del fondo y los textos en modo oscuro -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

</resources>
